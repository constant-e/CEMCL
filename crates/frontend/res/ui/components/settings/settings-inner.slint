import { HorizontalBox } from "std-widgets.slint";
import { VerticalSpacing } from "../spacing/spacing.slint";
export struct SettingString {
    id: string,
    name: string,
    value: string,
}

export struct SettingInt {
    id: string,
    name: string,
    value: int
}

export struct SettingBool {
    id: string,
    name: string,
    value: bool
}
